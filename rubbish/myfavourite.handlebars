    
    <div class="container">

        <div class='jumbotron'>
            <h1>我收藏的帖子 (List of favourite)</h1>
            <p>See the posts you starred !!</p>
        </div>

        <div class="row text-center">
            {{#each posts}}
                <div class="col-md-3 col-sm-6">
                    <div class="img-thumbnail" >
                        {{#if victim}}<p style="font-size:15px">匿名被害者(victim)</p>{{else}}<p style="font-size:15px">匿名犯匪者(criminal)</p>{{/if}}
                        <div class="caption"><p style="font-size:10px">{{ created_at }}</p></div>
                        <div class="caption"><h4>{{ title }}</h4></div>
                        {{#if image_path}}
                        <img src="{{ image_path }}" class="img-thumbnail">
                        {{/if}}
                        {{!-- ~~how to deal wif the content show ng show? (as discussed content shd show in postdetails instead of here) --}}
                        <div class="caption"><p>{{ content }}</p></div>
                        <p>[no.of support]</p>
                        <p>[no. of disupport]</p>
                        <p>[no. of advices]</p>
                        {{!-- ~~the button so ugly, suppose no button(just show ju that can click into details) just click on the thumbnail --}}
                        {{!-- <button>Details</button> --}}
                        <button><a href="/posts/{{id}}">Details</a></button>
                    </div>
                </div>
            {{/each}}  
        </div>

    </div>
