<div class='jumbotron detailBanner overlay' style="border-radius: 0px;height: 250px;display: flex;flex-direction: column;justify-content: center;">
</div>

<div class="container">
    <h1>我的留言</h1>
    <br/>

    <div id="minheight">

        <div class="row text-center">
            {{#each advices}}
            <div class="col-md-3 col-sm-6 " style="margin: 1px 0 1em;    margin-bottom: 17px;">
                <div class=" img-thumbnail ">
                    {{!-- {{#if victim}}
                    <p style="font-size:15px ">匿名被害者(victim)</p>{{else}}
                    <p style="font-size:15px ">匿名犯匪者(criminal)</p>{{/if}} --}}
                    <div class="caption ">
                        <p style="font-size:10px ">PostID : {{ post_id }}</p>
                        <p style="font-size:10px; margin-bottom: 17px; ">{{ created_at }}</p>

                    </div>
                    <div class="caption " style="margin-bottom: 17px" ;>
                        <p>{{ content }}</p>
                    </div>
                    <a class="btn btn-info btn-sm btn-block " href="/posts/{{post_id}} ">詳細內容</a>
                </div>
            </div>
            {{/each}}
        </div>
    </div>
</div>